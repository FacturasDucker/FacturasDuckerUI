// Reexporta los operadores que causan problemas
export { map as timestamp } from 'rxjs/operators';
export { map as toArray } from 'rxjs/operators';
export { map as window } from 'rxjs/operators';
export { map as windowCount } from 'rxjs/operators';
export { map as windowTime } from 'rxjs/operators';
